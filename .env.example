# Optional: Google OAuth 2.0 Web Client ID for Sign in with Google.
# Create it in Google Cloud Console, then restart `npm start` or rebuild.
GOOGLE_CLIENT_ID=your-google-web-client-id.apps.googleusercontent.com
